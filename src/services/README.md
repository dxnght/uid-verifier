# Business logic services go here.
#
# Convention: one file per domain concern (e.g. verification.ts, quota.ts, generator.ts).
# Services should not depend on Telegraf — they take plain data, return plain data.
# This keeps flows thin (Telegram I/O only) and logic testable.
